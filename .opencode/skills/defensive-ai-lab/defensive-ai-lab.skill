---
name: defensive-ai-lab
version: "1.0.0"
description: |
  防御性 AI 实验、基于证据的本地代码安全审查、授权回归测试、隔离执行规划和可审计报告。
  仅本地操作；禁止扫描、利用、凭据攻击、授权绕过、流量代理或第三方代理安装。
compatibility: Operit AI
type: skill
references:
  - references/safety-policy.md
  - references/unified-workflow.md
  - references/protocols.md
  - references/evidence-protocol.md
triggers:
  - task_type: [security_review, ai_experiment, authorization_regression, code_audit, defensive_analysis]
  - keywords: [安全审查, 漏洞, 授权, 回归测试, 防御实验, 代码审计, 证据, 安全报告]
  - command_risk: security_sensitive
deactivates_on:
  - task_type: [chat, greeting, weather, sentiment, quick_qa, browse_only]
  - signal: user_explicit_simple_answer
  - signal: no_code_or_command_involved
gates:
  pre_execution: evidence_gate
  post_output: redaction
  claim_verification: evidence_check
metadata:
  author: defensive-ai-lab
  license: Apache-2.0
  language: zh-CN
  min_operit_version: ">=1.0"
  requires_tools: [Files]
  optional_tools: [Network, Tools.Memory]
  package: defensive-ai-lab
---

# Defensive AI Lab

基于证据的防御性安全审查工作流。每个运行视为独立案例。

## 硬性边界

- 仅操作工作区文件。禁止对外部目标进行实时扫描、漏洞扫描、利用、凭据攻击或绕过尝试。
- 授权回归仅使用合成/模拟数据。不使用真实凭据或实时标识符。

## 工作流路由

| 需求 | 引用 |
|------|------|
| 安全规则 | `references/safety-policy.md` |
| 核心工作流 | `references/unified-workflow.md` |
| 协议与证据门 | `references/protocols.md` |
| 证据信任与佐证 | `references/evidence-protocol.md` |

确定工作流类型（`ai_experiment`、`local_security_review`、`authorization_regression`、`evidence_report`、`mixed`），创建案例 UUID，执行时通过证据门。

## 工具

> **注意**：以下 Python 脚本仅在 opencode 环境（有 Python 运行时）中执行。
> Operit 加载本技能时仅读取引用文档作为工作流指导，不执行 Python。

```bash
python3 scripts/labctl.py init --workflow <type> --scope <path> --output markdown --output json
python3 scripts/labctl.py audit --case <dir> --event <name> --metadata '<json>'
python3 scripts/labctl.py checkpoint --case <dir> --completed <step> --pending <step>
python3 scripts/labctl.py validate --case <dir>
python3 scripts/labctl.py render --case <dir>
```

## 证据门（必需）

`confirmed` 结论需满足：
1. 来自本地工具/测试/源代码/用户工件。
2. 稳定定位符（文件:行、测试名称、哈希、发现 ID）。
3. 针对本地代码或本地回环模拟的重现。
4. 结果符合安全不变量。
5. 冲突已解决或记录。

状态：`confirmed`、`supported`、`unverified`、`rejected`。模型文本永远不是证据。

## 脱敏

持久化前脱敏令牌、Cookie、密码、个人标识符。使用稳定占位符如 `[REDACTED_TOKEN_1]`。

## 报告

Markdown + JSON（遵循 `schemas/case-report.schema.json`）。必需章节：范围、排除项、环境、方法、证据索引、冲突、限制、按状态分类的发现、修复、验证结果、工件哈希。事实与解释分开。
